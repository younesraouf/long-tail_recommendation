STDIN
# Long Tail Recommendation (MORS Framework)

Implémentation de l'approche **Multi-Objective Optimization for Long Tail Recommendation** (Wang et al., 2016).

## Structure
- `src/`: Code source du projet.
- `data/`: Datasets (MovieLens).

## Installation
```bash
pip install -r requirements.txt
```
